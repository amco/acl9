cache: bundler
language: ruby
rvm:
  - 2.3.0
  - ruby-head

gemfile:
  - gemfiles/rails_5.0.gemfile
  - gemfiles/rails_5.1.gemfile

before_install:
  - gem update --system

matrix:
  fast_finish: true
